#!/usr/bin/env bash

powershell -NoProfile -ExecutionPolicy Bypass -File scripts/check-secrets.ps1 -StagedOnly
status=$?
if [ $status -ne 0 ]; then
  echo "pre-commit: blocked by secret check"
  exit $status
fi

exit 0
